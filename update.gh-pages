#!/bin/bash
#
# ~/dev/gh-pages/xlreg_py/update.gh-pages
#
cd ~/dev/gh-pages/xlreg_py
echo "you should have edited index.md and checked the results in index.html"
git add index.html
git commit -m "updating index.html"
git push origin gh-pages
